# 미팅 다중 참여

- 이슈: #111
- Epic: #107 프로젝트 기능
- 상태: draft
- 작성일: 2026-02-10

## 개요

여러 사용자가 동시에 하나의 미팅에 참여할 수 있는 기능.

## 요구사항

- [ ] 여러 사용자가 동일 미팅에 WebSocket으로 접속
- [ ] 참여자 목록 실시간 동기화
- [ ] 참여자별 권한 관리 (호스트/참여자)

## 인터페이스

### WebSocket

```
WS /ws/{room_id}?token={access_token}
```

#### 이벤트

| 이벤트 | 방향 | 설명 |
|--------|------|------|
| `join` | Client → Server | 미팅 참가 |
| `chat.send` | Client → Server | 채팅 메시지 전송 |
| `meeting.control` | Client → Server | 미팅 제어 (시작/종료/일시중지) |
| `agent.stream` | Server → Client | 에이전트 응답 스트리밍 |
| `meeting.state` | Server → Client | 미팅 상태 변경 알림 |
| `agenda.updated` | Server → Client | 안건 상태 변경 알림 |

### REST API

#### 미팅 목록 조회

```
GET /api/projects/{id}/meetings
```

**Query Parameters:**

| 파라미터 | 타입 | 기본값 | 설명 |
|---------|------|-------|------|
| `page` | int | 1 | 페이지 번호 |
| `limit` | int | 20 | 페이지 당 개수 |
| `status` | string | - | 상태 필터 (`created`, `in_progress`, `completed`) |

## 관련 코드

- `thetable/graph/workflow.py`
- `thetable/graph/state.py`
